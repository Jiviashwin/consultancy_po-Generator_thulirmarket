# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
build
dist

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── DevOps / Docker / Kubernetes ─────────────────────────
# IMPORTANT: Never commit k8s/secrets.yaml with real values.
# Use k8s/secrets.local.yaml instead (gitignored below):
k8s/secrets.local.yaml

# Jenkins data (persistent volume — never commit this)
jenkins/jenkins_home/
jenkins/*.log

# Minikube local state
.minikube/

# ── Terraform ─────────────────────────────────────────────
# Local provider plugins/cache
terraform/.terraform/
terraform/.terraform.lock.hcl

# State files (contain secrets in plaintext — NEVER commit)
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfstate.*.backup

# Your local variable values (copy from terraform.tfvars.example)
terraform/terraform.tfvars
